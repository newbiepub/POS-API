#Root Query
type Query {
    currentUser: User,
    products(limit: Int, skip: Int): [Product]
    categories(limit: Int, skip: Int): [Category]
    currentUserInventory: Inventory
    getAllPOS: [User]
}