#User Profile fields
type UserProfile {
    name: String
    address: String
    phoneNumber: String
}

#Role
type Role {
    _id: String
    roleName: String
}

interface User {
    _id: String!
    profile: UserProfile
}

#Get Current Company
type CurrentCompany implements User {
    _id: String!
    roles: [Role]
    profile: UserProfile
    email: String,
    status: String
}

#Get Current Employee
type CurrentEmployee implements User {
    _id: String!
    roles: [Role]
    profile: UserProfile
    username: String
    companyId: String,
    status: String
}

type ResponseUser {
    _id: String!
    roles: [Role]
    profile: UserProfile
    username: String
    status: String
}